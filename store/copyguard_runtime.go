package store

// Retired runtime capability. Historical schema/read APIs remain compatible;
// this is deliberately not a user/env switch that can restart simulation.
const CopyGuardShadowRuntimeEnabled = false
